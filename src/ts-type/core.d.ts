// 地图接口
interface MapOptions {
    zoom: number;
    pitch: number;
    rotation: number;
    center: [number, number];
}

interface ViewStatus {
    zoom: number;
    pitch: number;
    rotation: number;
    center: [number, number];
}
